<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rap Blog Oai" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Rap Blog Oai">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header">
      <div class="site-header__inner">
        <div class="site-header__brand">
          <%= link_to "Rap Blog", root_path %>
        </div>
        <nav class="site-header__nav">
          <%= link_to "НАЙТИ", find_path, class: "button button--ghost" %>
          <%= form_with url: posts_path, method: :get, class: "site-search" do |form| %>
            <% if params[:tag].present? %>
              <%= form.hidden_field :tag, value: params[:tag] %>
            <% end %>
            <%= form.search_field :q, value: params[:q], placeholder: "Search...", aria: { label: "Search posts and comments" } %>
          <% end %>
          <% if logged_in? %>
            <details class="user-menu">
              <summary class="button button--ghost user-menu__summary">
                <span class="user-menu__name"><%= current_user.email %><%= " · admin" if admin? %></span>
                <span class="user-menu__chevron">▾</span>
              </summary>
              <div class="user-menu__panel">
                <%= link_to "Edit profile", edit_account_path, class: "user-menu__item" %>
                <%= button_to "Sign out", logout_path, method: :delete, class: "user-menu__item user-menu__item--danger", form: { class: "user-menu__form" } %>
              </div>
            </details>
          <% else %>
            <%= link_to "Sign in", login_path, class: "button button--ghost" %>
            <%= link_to "Create account", signup_path, class: "button" %>
          <% end %>
        </nav>
      </div>
    </header>

    <% flash.each do |type, message| %>
      <div class="flash flash--<%= type %>"><%= message %></div>
    <% end %>

    <% if params[:tag].present? %>
      <div class="active-filter">
        <span>Tag:</span>
        <span class="tag tag--active">#<%= params[:tag] %></span>
        <% clear_tag_path = action_name == "find" ? find_path(q: params[:q], sort: params[:sort], from: params[:from], to: params[:to], user_id: params[:user_id]) : posts_path(q: params[:q]) %>
        <%= link_to "Clear tag", clear_tag_path, class: "button button--ghost" %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
